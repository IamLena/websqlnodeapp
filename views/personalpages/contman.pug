extends ../layout
block content
	div.container
		div.row
			div.col-sm
				img.personalpic(src='/images/anon.png')
			div.col-sm
				h4 content manager
				h1 #{firstname} #{lastname}
				p #{lan_geo}
				div
					a.btn.btn-info(href='#') create
					a.btn.btn-info(href='#') modify/publish
				form.bottom(action="/auth/logout", method="post")
					button.btn.btn-secondary log out
