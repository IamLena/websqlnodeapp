extends layout
block content
	form(method="post", action="/file/upload", enctype="multipart/form-data")
		label(for="inputfile") input file
		input(type='file', id="inputfile", name="myfile")
		br
		button(type='submit') submit
